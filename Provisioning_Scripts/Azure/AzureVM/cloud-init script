# Paste below to custom data field when deploying Azure VM - this will deploy the basic tools (docker, kubectl, etc.) 
# Also make sure to add your IP / create VPC to allow inbound traffic for SSH and RDP

#!/bin/bash
bash <(curl -s https://raw.githubusercontent.com/JustasBud/kubernetes/main/Provisioning_Scripts/Azure/AzureVM/Azure.sh)